//import React, { useContext } from 'react';
//import { Shop } from '../../contex/ShopProv';
import './estilos.css';


const Cart = () => {
  //const {cart} = useContext(Shop);

  return (
<div className='cart'>
  CART
</div>
    /*<ul>
      {cart.map(producto => {
        return <li key={Math.random()}>{producto.title} <img src={producto.img} width='80px' alt='producto'/></li>
      })}
    </ul>*/
  )
}

export default Cart